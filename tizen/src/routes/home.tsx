import LibrariesRow from '../components/LibrariesRow';

const Home = () => {
    return (
        <div className="flex flex-col items-start gap-6">
            <LibrariesRow />
        </div>
    );
};

export default Home;
